{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter @workspace/mogged run build",
  "outputDirectory": "artifacts/mogged/dist/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
